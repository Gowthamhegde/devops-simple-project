apiVersion: v1
kind: Service
metadata:
  name: nginx-lb-service
  namespace: release-board
  labels:
    app: nginx-lb
spec:
  type: NodePort
  selector:
    app: nginx-lb
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
